# Este arquivo deve estar vazio em produção
# Todas as configurações necessárias são gerenciadas pela Vercel

# Para desenvolvimento local, use .env.local com as seguintes variáveis:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
# MONGODB_DB=ibovcontrol
# JWT_SECRET=your-super-secret-jwt-key-min-32-characters

# Variáveis opcionais (o app funciona sem elas):
# VITE_GOOGLE_API_KEY=sua_chave_api_google (fallback para planilha pública se não informada)
